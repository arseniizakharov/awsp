# Support

Use GitHub issues for bugs, feature requests, and documentation problems.

Before filing a bug:

```sh
awsp doctor
awsp current
awsp status
```

Redact AWS account IDs, SSO start URLs, and any token/cache details before posting output.

For security-sensitive reports, use the process in `SECURITY.md` instead of a public issue.
